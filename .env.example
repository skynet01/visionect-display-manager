# Copy this file to .env before running docker compose.
# Set TZ to the same timezone you want the scheduler and admin UI to use.
# PUID / PGID should match the local user that will own generated files.
TZ=America/Phoenix
PUID=1000
PGID=1000
